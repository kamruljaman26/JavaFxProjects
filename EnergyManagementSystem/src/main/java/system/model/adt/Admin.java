package system.model.adt;

public class Admin extends User {
    public Admin(String name, String phoneNum, String curAddress, String username, String password) {
        super(name, phoneNum, curAddress, username, password);
    }
}
